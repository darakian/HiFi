//! # HiFi
//!
//! `HiFi` is a collection of functions and structs to aid in analysing URL endpoints.

pub fn generate_inputs(pattern: String) -> Vec<String>{
    let the_set = Vec::new();
    the_set
}